
package org.ngi.nodes.math.other;

import org.ngi.nodes.math.BaseFunction;

/**
 * Sqrt math function.  Finds the square root of a number.
 * @author Tom
 */
public class SquareRoot extends BaseFunction {
    public SquareRoot() {
        super( "SquareRoot", "sqrt", false, "Square Root function, finds the square root of the number.");
    }    
}
