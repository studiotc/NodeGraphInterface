package org.ngi.nodes.math.operators;


import org.ngi.nodes.math.BaseOperator;

/**
 *  Subtraction operator (-).  Subtracts the second number (b) from the first number (A).
 * @author Tom
 */
public class Subtract extends BaseOperator {
    
    
    /**
     * Constructor
     * 
     */
    public Subtract() {
        super("Subtract", "-", "Subtraction operator (-).");
    }
    
     
}
